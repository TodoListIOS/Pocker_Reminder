# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'Pocker_Reminder' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # pod 'JSONKit',	'~> 1.4'
  #pod 'SWiftJson', '~>1.0'
  pod 'Alamofire'
  # Pods for Pocker_Reminder

  target 'Pocker_ReminderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Pocker_ReminderUITests' do
    # Pods for testing
  end

end
